<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title><%= title || 'Shiba Jurídico' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-light">
  <div class="d-flex flex-column min-vh-100">

    <!-- Header escuro -->
    <header class="p-3 border-bottom bg-dark">
      <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-between">
          <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
            <strong>🏛️ Shiba Jurídico</strong>
          </a>
          <ul class="nav">
            <li class="nav-item"><a href="/" class="nav-link px-2 text-white">Início</a></li>
            <li class="nav-item"><a href="/novo" class="nav-link px-2 text-white">Novo Post</a></li>
            <li class="nav-item"><a href="/agendar" class="nav-link px-2 text-white">Agendar</a></li>
            <li class="nav-item"><a href="/fila" class="nav-link px-2 text-white">Fila</a></li>
          </ul>
        </div>
      </div>
    </header>

    <!-- Conteúdo principal -->
    <main class="container py-4 flex-grow-1">
      <% if (mensagem && mensagem.length > 0) { %>
        <div class="alert alert-success text-center">
          <%= mensagem[0] %>
        </div>
      <% } %>

      <%- body %>
    </main>

  </div>

  <!-- Footer escuro fixado no fim -->
  <footer class="text-center py-4 bg-dark text-light border-top">
    <small>&copy; <%= new Date().getFullYear() %> Shiba Jurídico. Todos os direitos reservados.</small>
  </footer>

</body>
</html>
